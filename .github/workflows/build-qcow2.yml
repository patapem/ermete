name: Build and Inject Ermete OS (qcow2)

on:
  workflow_dispatch: # Mantiene il pulsante manuale per le emergenze
  workflow_run:
    workflows: ["Build and Push Fedora Atomic OS"] # Sostituisci questo con il 'name' esatto del file build-os.yml
    types:
      - completed # Si avvia solo quando l'altro ha finito
    branches:
      - testing-uki # Si avvia solo se l'altro workflow girava su questo branch

jobs:
  build-and-inject:
    # Esegue questo job SOLO se il workflow precedente ha avuto successo (evita di generare dischi se il container è fallito)
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    permissions:
      packages: read
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Installa Dipendenze (Podman e libguestfs)
        run: |
          sudo apt-get update
          sudo apt-get install -y podman libguestfs-tools

      - name: Login al GitHub Container Registry come Root
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | sudo podman login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build qcow2 tramite bootc-image-builder
        run: |
          mkdir -p output
          
          # Gestione dinamica dell'hash: se lanciato da un altro workflow prende l'hash di quello, altrimenti prende quello corrente
          if [ "${{ github.event_name }}" == "workflow_run" ]; then
            FULL_SHA="${{ github.event.workflow_run.head_sha }}"
          else
            FULL_SHA="${{ github.sha }}"
          fi
          
          SHORT_SHA=$(echo $FULL_SHA | cut -c1-7)
          IMAGE_REF="ghcr.io/patapem/ermete:$SHORT_SHA"
          
          echo "========================================="
          echo "1. Scaricamento dell'immagine OCI in locale: $IMAGE_REF"
          echo "========================================="
          sudo podman pull $IMAGE_REF
          
          echo "========================================="
          echo "2. Avvio build del disco qcow2 tramite osbuild"
          echo "========================================="
          sudo podman run --rm --privileged \
            -v $(pwd)/output:/output \
            -v /var/lib/containers/storage:/var/lib/containers/storage \
            quay.io/centos-bootc/bootc-image-builder:latest \
            build --type qcow2 --rootfs btrfs $IMAGE_REF

      - name: Iniezione Bootloader e UKI (Day-1 Automation)
        run: |
          echo "========================================="
          echo "3. Estrazione file dalla partizione di Root..."
          echo "========================================="
          mkdir -p /tmp/guest
          mkdir -p /tmp/extracted
          
          # Montaggio Root (BTRFS) in sola lettura
          sudo guestmount -a output/qcow2/disk.qcow2 -m /dev/sda4 --ro /tmp/guest
          
          echo "Estrazione file..."
          sudo find /tmp/guest/ -path "*/usr/lib/systemd/boot/efi/systemd-bootx64.efi" -exec cp {} /tmp/extracted/BOOTX64.EFI \;
          sudo find /tmp/guest/ -name "vmlinuz-ermete.efi" -exec cp {} /tmp/extracted/vmlinuz-ermete.efi \;
          
          sudo guestunmount /tmp/guest
          
          echo "========================================="
          echo "4. Iniezione nella partizione EFI..."
          echo "========================================="
          # Montaggio EFI (FAT32) in lettura/scrittura
          sudo guestmount -a output/qcow2/disk.qcow2 -m /dev/sda2 /tmp/guest
          
          sudo mkdir -p /tmp/guest/EFI/BOOT
          sudo mkdir -p /tmp/guest/EFI/Linux
          
          sudo cp /tmp/extracted/BOOTX64.EFI /tmp/guest/EFI/BOOT/
          sudo cp /tmp/extracted/vmlinuz-ermete.efi /tmp/guest/EFI/Linux/
          
          sudo sync
          sudo guestunmount /tmp/guest
          
          echo "Iniezione completata con successo! Disco armato."

      - name: Upload Artefatto (qcow2)
        uses: actions/upload-artifact@v4
        with:
          name: ermete-os-bootable-disk
          path: output/qcow2/disk.qcow2
          retention-days: 7
